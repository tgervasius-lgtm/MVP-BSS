{
  "name": "bss-smart-systems-demo",
  "version": "3.0.0",
  "private": true,
  "scripts": {
    "test": "node --test tests/app.test.cjs",
    "build": "node scripts/build.mjs",
    "serve:dist": "node scripts/serve-dist.mjs",
    "start:fullstack": "npm run build && npm --prefix backend run build && FRONTEND_ROOT=../dist npm --prefix backend run start",
    "lint": "eslint .",
    "pretest:e2e": "npm run build",
    "test:e2e": "playwright test",
    "check": "npm run lint && npm test && npm run build",
    "ci": "npm run lint && npm test && npm run test:e2e"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.12.1",
    "@eslint/js": "^10.0.1",
    "@playwright/test": "^1.61.1",
    "eslint": "^10.7.0",
    "jsdom": "26.1.0"
  }
}
