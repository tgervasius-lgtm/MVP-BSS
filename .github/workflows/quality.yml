name: BSS quality gate

on:
  pull_request:
    branches: [main]
  push:
    branches:
      - 'agent/**'

permissions:
  contents: read

concurrency:
  group: fullstack-quality-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  quality:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: bss_e2e
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U postgres -d bss_e2e"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10
    steps:
      - name: Checkout
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6

      - name: Node 22
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 22
          cache: npm

      - name: Install frontend dependencies without lifecycle or implicit audit scripts
        run: npm ci --ignore-scripts --no-audit

      - name: Install backend dependencies without lifecycle or implicit audit scripts
        run: npm --prefix backend ci --ignore-scripts --no-audit

      - name: Rebuild required backend esbuild binary explicitly
        run: npm --prefix backend rebuild esbuild --foreground-scripts

      - name: Architecture growth guard
        run: node scripts/check-architecture-budgets.mjs

      - name: Lint
        run: npm run lint

      - name: Unit and integration tests
        run: npm test

      - name: Build Cloudflare Pages output
        run: npm run build

      - name: Build backend
        run: npm --prefix backend run build

      - name: Migrate PostgreSQL
        run: npm --prefix backend run migrate:prod
        env:
          NODE_ENV: test
          PUBLIC_ORIGIN: http://127.0.0.1:4173
          DATABASE_URL: postgres://postgres:postgres@127.0.0.1:5432/bss_e2e
          DATABASE_SSL: "false"

      - name: Bootstrap E2E organization
        run: npm --prefix backend run bootstrap:prod
        env:
          NODE_ENV: test
          PUBLIC_ORIGIN: http://127.0.0.1:4173
          DATABASE_URL: postgres://postgres:postgres@127.0.0.1:5432/bss_e2e
          DATABASE_SSL: "false"
          BSS_BOOTSTRAP_ORGANIZATION_NAME: BSS E2E d.o.o.
          BSS_BOOTSTRAP_ADMIN_EMAIL: admin-e2e@bss.test
          BSS_BOOTSTRAP_ADMIN_PASSWORD: E2E-secure-password-2026!

      - name: Install Chromium
        run: ./node_modules/.bin/playwright install --with-deps chromium

      - name: Browser E2E and axe
        run: npm run test:e2e -- --reporter=line
        env:
          BSS_E2E_FULLSTACK: "true"
          BSS_E2E_BASE_URL: http://127.0.0.1:4173
          NODE_ENV: test
          HOST: 127.0.0.1
          PORT: "4173"
          PUBLIC_ORIGIN: http://127.0.0.1:4173
          DATABASE_URL: postgres://postgres:postgres@127.0.0.1:5432/bss_e2e
          DATABASE_SSL: "false"
          COOKIE_SECURE: "false"
          FRONTEND_ROOT: ../dist
          RFID_UID_PEPPER: e2e-rfid-pepper-0123456789abcdef
          DEVICE_CREDENTIAL_ENCRYPTION_KEY: e2e-device-encryption-key-0123456789abcdef
          TERMINAL_ACTIVATION_CODE: e2e-terminal-activation-code
          BSS_BOOTSTRAP_ADMIN_EMAIL: admin-e2e@bss.test
          BSS_BOOTSTRAP_ADMIN_PASSWORD: E2E-secure-password-2026!
